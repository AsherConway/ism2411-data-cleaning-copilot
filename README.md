This project shows a data cleaning process in VS code for messy sales data. the script dealt with issues including handling missing values, removing invalid rows, and standardizing column names.

when the code is ran, a sales_data_clean file with show under processed, this is the clean data set